# Ambiente de Teste - ConectaSEI v2.0
# Este arquivo configura o ambiente de testes com Docker Compose

# ========================================
# ENVIRONMENT
# ========================================
ENVIRONMENT=test

# ========================================
# DATABASE (PostgreSQL + ParadeDB)
# ========================================
# Conexão PostgreSQL (use o mesmo do dev para testes locais: docker-compose = 5432)
# Para CI com banco isolado, use porta 5433 e usuário/senha de teste
DATABASE_URL=postgresql://automasei:automasei_dev_password@localhost:5432/automasei

# ========================================
# FIREBASE STORAGE (Emulator)
# ========================================
# Aponta para Firebase Emulator local
FIREBASE_STORAGE_EMULATOR_HOST=localhost:9199
FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
FIRESTORE_EMULATOR_HOST=localhost:8080

# Projeto de teste (não precisa existir no Firebase real)
GCLOUD_PROJECT=conectasei-test
FIREBASE_STORAGE_BUCKET=conectasei-test.appspot.com

# Credenciais fake para emulator (não são validadas)
FIREBASE_CREDENTIALS=tests/firebase/fake-credentials.json

# ========================================
# AUTH (dev user sem token nos testes de API)
# ========================================
AUTH_DEV_MODE=true

# ========================================
# API SETTINGS
# ========================================
API_HOST=0.0.0.0
API_PORT=8001
API_RELOAD=False

# ========================================
# SCHEDULER (Desabilitado em testes)
# ========================================
SCHEDULER_ENABLED=False

# ========================================
# LOGGING
# ========================================
LOG_LEVEL=DEBUG
