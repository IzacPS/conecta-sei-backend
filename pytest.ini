[pytest]
pythonpath = .
testpaths = tests
markers =
    e2e: E2E tests with Playwright (require API + frontend running). Run with: pytest -m e2e
    scraping: Scraping tests with Playwright + HTML fixtures (no live SEI)