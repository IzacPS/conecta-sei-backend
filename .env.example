# ConectaSEI v2.0 - Variaveis de ambiente
# Copie para .env e preencha. Em Docker, use env_file ou variaveis no compose.

# ── Banco (obrigatorio) ──
# Docker: use mesmo user/senha do docker-compose (automasei para compatibilidade com volume)
DATABASE_URL=postgresql://automasei:automasei_dev_password@localhost:5432/automasei

# ── Criptografia (credenciais SEI) ──
# Gere uma chave: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CONECTASEI_ENCRYPTION_KEY=

# ── Modo dev (sem Firebase) ──
# true = API aceita requests sem token e usa usuario fixo dev@conectasei.local
AUTH_DEV_MODE=true

# ── CORS ──
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ── Pagamentos ──
PAYMENT_PROVIDER=manual

# ── Email / Notificacoes ──
# Destinatarios (fallback se nao houver SystemConfiguration notification_emails)
# NOTIFICATION_EMAILS=email1@example.com,email2@example.com
# SMTP (Gmail, servidor proprio, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASSWORD=app-password-aqui
# SMTP_FROM=noreply@conectasei.com

# ── MongoDB (apenas para migração do legacy) ──
# Usado por: scripts/migrate-mongo-to-postgres.py
# DICA: não commite isso; use apenas no seu .env local.
# MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/?retryWrites=true&w=majority
# MONGO_DB_NAME=sei_database

# ── Firebase (Auth + Storage) ──
# Conteúdo do JSON da service account (minificado em uma linha). Não use arquivo.
# Ex.: FIREBASE_CREDENTIALS={"type":"service_account","project_id":"...",...}
FIREBASE_CREDENTIALS=
FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# ── Frontend (Nuxt) ──
# NUXT_PUBLIC_API_BASE=http://localhost:8000

# ── Producao ──
# AUTH_DEV_MODE=false
# HTTP_PORT=80
